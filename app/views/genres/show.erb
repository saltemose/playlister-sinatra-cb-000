<ul>
  <% @genre.artists.each do |artist| %>
  <li><%= artist.name %></li>
  <a href= <%="artists/#{artist.slug}"%>>Click here to go to the Artist's page</a>
  <% end %>
</ul>

<ul>
<% @genre.songs.each do |song| %>
<li><%= song.name %></li>
<a href=<%="songs/#{song.slug}"%>>Click here for the song's page</a>
<% end %>
</ul>
